# During local development when frontend and backend are served separately:
VITE_API_URL=http://localhost:5000/api
# Use the same origin in production when the frontend is served by Express/Render.
# VITE_API_URL=/api
VITE_UPLOADS_URL=
