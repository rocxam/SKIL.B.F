{
  "name": "skillbridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd skillbridge-backend && npm install && npm run build",
    "seed": "cd skillbridge-backend && npm run seed:postgres",
    "start": "cd skillbridge-backend && npm run seed:postgres && npm run start"
  }
}
